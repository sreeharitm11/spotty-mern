{
  "version": 2,
  "builds": [
    {
      "src": "client/spotty-client/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "client/spotty-client/dist"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
